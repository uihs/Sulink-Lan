// Package gui 提供图形界面客户端（Windows）。
//
// 本文件放平台无关的常量，保证非 Windows 平台也能编译通过。
package gui

// appVersion 客户端版本号（展示在设置页）。
const appVersion = "1.0.0"

// protocolVersion 协议版本（展示在设置页）。
const protocolVersion = "v1"
